package com.globalpulse.news.presentation.theme

import androidx.compose.material3.Typography

// Uses the platform default type scale rather than bundling custom font
// files — keeps the build self-contained with no binary font assets to
// source or license. Swap in a custom FontFamily here later if desired.
val GlobalPulseTypography = Typography()

org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
android.nonTransitiveRClass=true

# Backend base URL, read into BuildConfig.BASE_URL by app/build.gradle.kts.
# Edit THIS file (not the Kotlin code) once your backend is hosted publicly.
# Must end with a trailing slash and be https:// (cleartext traffic is blocked).
BASE_URL=https://REPLACE-ME-your-backend-host.example.com/
